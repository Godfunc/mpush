[package]
name = "mpush"
version = "0.1.0"
edition = "2024"

[dependencies]
ureq = { version = "2", default-features = false, features = ["tls"] }
libc = "0.2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
